const { PrismaClient } = require('./node_modules/@prisma/client'); const prisma = new PrismaClient(); async function main() { const accounts = await prisma.socialAccount.findMany({ orderBy: { createdAt: 'desc' }, take: 10 }); console.log('Recent Accounts:', accounts.map(a => ({ id: a.id, name: a.accountName, provider: a.provider, providerAccountId: a.providerAccountId }))); } main().catch(console.error).finally(() => prisma.$disconnect());
